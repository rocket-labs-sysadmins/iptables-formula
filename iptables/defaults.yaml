pkgs:
  Debian: 
    - iptables
    - iptables-persistent
  RedHat:
    '7':
      - iptables
      - iptables-services
    '6':
      - iptables
      - iptables-ipv6
  default: 'RedHat'

iptables:
  flush: False
  firstrunflush: True
  strict: False
  install: False
