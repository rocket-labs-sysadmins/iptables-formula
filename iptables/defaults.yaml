pkgs:
  Debian: 
    - iptables
    - iptables-persistent
  RedHat:
    - iptables
    - iptables-services
  default: 'RedHat'

iptables:
  flush: False
  firstrunflush: True
  strict: False
  install: False
